import 'package:flutter/foundation.dart';
// import 'package:guident_admin/model/school_detail_model.dart';
// import 'package:guident_admin/services/user_service.dart';

/*class SchoolDetailProvider extends ChangeNotifier {
  SchoolDetailModel? _model;
  SchoolDetailModel? get model => _model;
  bool _isLoading = true;
  bool get isLoading => _isLoading;
  bool get isEmpty => _model!.data == null;

  getDetail() async {
    try {
      _model = await UserService().getSchool();
      _isLoading = false;
      notifyListeners();
    } catch (e) {
      _isLoading = false;
      notifyListeners();
    }
  }
}*/
